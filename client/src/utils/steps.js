const steps = {
    'bubblesort':[
        "for(i=0;i<arr.length<i++) {",
        "   for(j=0;j<arr.length-i;j++) {",
        "       if(arr[j] > arr[j+1]) {",
        "           swap(arr[j],arr[j+1])",
        "       }",
        "   }",
        "}"
    ],
    'selectionsort':[],
}

export default steps;

